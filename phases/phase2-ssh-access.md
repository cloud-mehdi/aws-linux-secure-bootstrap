# Phase 2 – First SSH Access

The EC2 instance was accessed from a macOS terminal using SSH key-based authentication.

Command used:
ssh -i ~/.ssh/mehdi-aws.pem ubuntu@<EC2_PUBLIC_IP>

The connection confirmed:
- Correct key pair usage
- Public IP connectivity
- Secure, passwordless authentication

